controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

api_auth:
    path: /api/auth
    methods: ['POST']

api_refresh_token:
    path: /api/token/refresh

api_doc:
    path: /api/docs
    controller: api_platform.swagger_ui.action
